"""Ruleset / blocker engine package — base in :mod:`ruleset.base`.

The real engine (command/path/network/capability/cost rules) is added by a feature unit
and installed via :func:`ruleset.base.set_ruleset`.
"""
from __future__ import annotations
