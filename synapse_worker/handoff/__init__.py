"""Native Handoff Protocol (possible-features §11) — the daemon-local handoff broker,
runner, MCP server, and chain-grant cache. A constrained sibling of the §2 orchestrator:
sequential baton-pass only (no create/edit, no fan-out)."""
