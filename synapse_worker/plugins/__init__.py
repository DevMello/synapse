"""Plugin runtime package — manifest + registry in :mod:`plugins.base`.

The install/lifecycle machinery (venv provisioning, checksum verify, MCP process mgmt)
is added by the plugin unit in ``plugins/runtime.py``.
"""
from __future__ import annotations
