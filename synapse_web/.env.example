# Supabase (Auth + Realtime + data API). The Web UI talks to Supabase directly
# as an external service; REST is same-origin (served by the Cloud Backend host).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Same-origin REST base; empty = current origin (cloud host serves the bundle).
VITE_API_BASE=
