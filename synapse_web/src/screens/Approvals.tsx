import { ScreenStub } from "../components/Common";

// Approvals — STUB: a worker fills this in (see plan unit).
export default function Approvals() {
  return <ScreenStub name="Approvals" note="HITL queue" />;
}
