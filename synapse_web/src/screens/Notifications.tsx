import { ScreenStub } from "../components/Common";

// Notifications — STUB: a worker fills this in (see plan unit).
export default function Notifications() {
  return <ScreenStub name="Notifications" note="channels + routing" />;
}
