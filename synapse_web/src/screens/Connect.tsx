import { ScreenStub } from "../components/Common";

// Connect a device — STUB: a worker fills this in (see plan unit).
export default function Connect() {
  return <ScreenStub name="Connect a device" note="device-code pairing" />;
}
