import { ScreenStub } from "../components/Common";

// Settings — STUB: a worker fills this in (see plan unit).
export default function Settings() {
  return <ScreenStub name="Settings" note="org, members, billing, tokens" />;
}
