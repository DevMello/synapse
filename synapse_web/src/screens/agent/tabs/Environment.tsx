import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Environment tab. STUB: a worker fills this in (see plan unit).
export default function EnvironmentTab() {
  return <ScreenStub name="Environment" note="agent detail tab" />;
}
