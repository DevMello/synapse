import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Analytics tab. STUB: a worker fills this in (see plan unit).
export default function AnalyticsTab() {
  return <ScreenStub name="Analytics" note="agent detail tab" />;
}
