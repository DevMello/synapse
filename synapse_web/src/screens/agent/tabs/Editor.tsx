import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Editor tab. STUB: a worker fills this in (see plan unit).
export default function EditorTab() {
  return <ScreenStub name="Editor" note="agent detail tab" />;
}
