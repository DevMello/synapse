import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Versions tab. STUB: a worker fills this in (see plan unit).
export default function VersionsTab() {
  return <ScreenStub name="Versions" note="agent detail tab" />;
}
