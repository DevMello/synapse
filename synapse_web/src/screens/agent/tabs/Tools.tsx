import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Tools & MCP tab. STUB: a worker fills this in (see plan unit).
export default function ToolsTab() {
  return <ScreenStub name="Tools & MCP" note="agent detail tab" />;
}
