import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Plugins tab. STUB: a worker fills this in (see plan unit).
export default function PluginsTab() {
  return <ScreenStub name="Plugins" note="agent detail tab" />;
}
