import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Schedule tab. STUB: a worker fills this in (see plan unit).
export default function ScheduleTab() {
  return <ScreenStub name="Schedule" note="agent detail tab" />;
}
