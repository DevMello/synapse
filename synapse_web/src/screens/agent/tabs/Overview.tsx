import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Overview tab. STUB: a worker fills this in (see plan unit).
export default function OverviewTab() {
  return <ScreenStub name="Overview" note="agent detail tab" />;
}
