import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Logs tab. STUB: a worker fills this in (see plan unit).
export default function LogsTab() {
  return <ScreenStub name="Logs" note="agent detail tab" />;
}
