import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Runs tab. STUB: a worker fills this in (see plan unit).
export default function RunsTab() {
  return <ScreenStub name="Runs" note="agent detail tab" />;
}
