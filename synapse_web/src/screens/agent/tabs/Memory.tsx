import { ScreenStub } from "../../../components/Common";
// import { useCurrentAgent } from "../context";

// Agent Detail — Memory tab. STUB: a worker fills this in (see plan unit).
export default function MemoryTab() {
  return <ScreenStub name="Memory" note="agent detail tab" />;
}
