import { ScreenStub } from "../components/Common";

// Webhooks — STUB: a worker fills this in (see plan unit).
export default function Webhooks() {
  return <ScreenStub name="Webhooks" note="inbound triggers" />;
}
