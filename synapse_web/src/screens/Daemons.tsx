import { ScreenStub } from "../components/Common";

// Daemons — STUB: a worker fills this in (see plan unit).
export default function Daemons() {
  return <ScreenStub name="Daemons" note="registered workers + uptime + revoke" />;
}
