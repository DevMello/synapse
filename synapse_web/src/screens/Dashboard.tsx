import { ScreenStub } from "../components/Common";

// Dashboard — STUB: a worker fills this in (see plan unit).
export default function Dashboard() {
  return <ScreenStub name="Dashboard" note="fleet overview" />;
}
