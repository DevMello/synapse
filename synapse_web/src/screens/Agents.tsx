import { ScreenStub } from "../components/Common";

// Agents — STUB: a worker fills this in (see plan unit).
export default function Agents() {
  return <ScreenStub name="Agents" note="list + one-click New Agent wizard" />;
}
