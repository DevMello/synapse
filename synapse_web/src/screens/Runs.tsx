import { ScreenStub } from "../components/Common";

// Runs — STUB: a worker fills this in (see plan unit).
export default function Runs() {
  return <ScreenStub name="Runs" note="global run history" />;
}
