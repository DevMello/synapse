import { ScreenStub } from "../components/Common";

// Marketplace — STUB: a worker fills this in (see plan unit).
export default function Marketplace() {
  return <ScreenStub name="Marketplace" note="agents + skills + plugins" />;
}
