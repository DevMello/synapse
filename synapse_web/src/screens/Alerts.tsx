import { ScreenStub } from "../components/Common";

// Alerts — STUB: a worker fills this in (see plan unit).
export default function Alerts() {
  return <ScreenStub name="Alerts" note="anomaly feed" />;
}
